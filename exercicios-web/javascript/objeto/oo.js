//  CÓDIGO NÃO EXECUTAVEL

// Procedural
processamento(valor1, valor2, valor3)

// OO
objeto = {
    valor1,
    valor2,
    valor3,
    processamento() {
        // ...
    }
}

objeto.processamento()// Foco passou a ser o objeto

// Objeto é a capsula que agrupa comportamento e atributos

/* Principios importantes:
    1. Abstração - É buscar trazer algo ou uma ideia para o programa de maneira que isso faça sentido e funcione
    2. Encapsulamento
    3. Herança(Prototype) - Em JavaScript não é possivel ter herança multipla, apenas cadeia de herança
    4. Polimorfismo*/